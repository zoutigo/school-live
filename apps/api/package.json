{
  "name": "@school-live/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; nest start --watch'",
    "worker:dev": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; nest start --watch --entryFile worker-main'",
    "media:dev": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; nest start --watch --entryFile media-main'",
    "build": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; nest build'",
    "start": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; node dist/main.js'",
    "worker:start": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; node dist/worker-main.js'",
    "media:start": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; node dist/media-main.js'",
    "prisma:generate": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; prisma generate --schema prisma/schema.prisma'",
    "prisma:migrate:dev": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; prisma migrate dev --schema prisma/schema.prisma'",
    "prisma:migrate:deploy": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; prisma migrate deploy --schema prisma/schema.prisma'",
    "prisma:studio": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; prisma studio --schema prisma/schema.prisma'",
    "seed:academic:backfill": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; node prisma/scripts/seed-academic-backfill.mjs'",
    "seed:feed:dev": "bash -lc 'if [ -f ../../docker/.env ]; then set -a; source ../../docker/.env; set +a; elif [ -f ../../.env ]; then set -a; source ../../.env; set +a; fi; node prisma/scripts/seed-feed-dev.mjs'",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "jest --config ./jest.config.cjs",
    "test:e2e": "bash ./scripts/run-e2e.sh"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/config": "^4.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/jwt": "^11.0.0",
    "@nestjs/passport": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "@prisma/client": "^6.4.1",
    "bcryptjs": "^2.4.3",
    "bullmq": "^5.69.2",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "cookie-parser": "^1.4.7",
    "ioredis": "^5.9.3",
    "nodemailer": "^8.0.1",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "sharp": "^0.34.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "@nestjs/testing": "^11.0.0",
    "@types/bcryptjs": "^2.4.6",
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.1",
    "@types/nodemailer": "^7.0.9",
    "@types/passport-jwt": "^4.0.1",
    "eslint": "^9.20.0",
    "jest": "^29.7.0",
    "prisma": "^6.4.1",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2"
  }
}
